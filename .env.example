# Copia este fichero a .env y ajusta los valores si lo necesitas.
# Con los valores por defecto, `docker compose up` funciona sin cambios.
# La base de datos es un único fichero SQLite persistido en un volumen Docker.

# Puerto expuesto en el host
APP_PORT=3000
