services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: file:/data/resume-builder.db
      PORT: 3000
    volumes:
      - app-data:/data
    ports:
      - '${APP_PORT:-3000}:3000'

volumes:
  app-data:
