# Por defecto la API se sirve en el mismo origen (/api): en desarrollo vía el
# proxy de Vite (ver vite.config.ts) y en producción porque el backend sirve
# también el frontend compilado. Solo hace falta sobreescribir esto si la API
# vive en otro origen.
# VITE_API_URL=http://localhost:3000/api
